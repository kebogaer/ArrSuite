services:
  arrsuite:
    build: .
    container_name: arrsuite
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data

volumes:
  arrsuite_data:
